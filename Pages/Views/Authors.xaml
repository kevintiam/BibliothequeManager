<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BibliothequeManager.Pages.Views.Authors"
             xmlns:localApp="clr-namespace:BibliothequeManager"
             Title="Authors"
             NavigationPage.HasNavigationBar="False">
    <Grid>
        <!-- Fond léger optionnel -->
        <Image x:Name="BackgroundImage"
               Source="new_background.png"
               Aspect="AspectFill"
               InputTransparent="True"
               Opacity="0.2" />

        <ScrollView>
            <Grid RowDefinitions="Auto,Auto,*,Auto">
                
                <!-- ========== EN-TÊTE ========== -->
                <Grid Style="{StaticResource StackLayoutAcc}">
                    <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Spacing="100">
                        <!-- Logo à gauche -->
                        <Border Style="{StaticResource RoundedImageBorder}">
                            <Image Source="logo.png" Style="{StaticResource Logo}" />
                        </Border>

                        <StackLayout Style="{StaticResource StackLayoutAcce}"
                                 x:Name="HeaderTextLayout">
                            <Label x:Name="HeaderTitle"
                               Text="{Binding [AuthorManagement], Source={x:Static localApp:App.Localized}}"
                                   
                               Style="{StaticResource HeaderLabel}" />
                            <Label x:Name="HeaderSubtitle"
                               Text="{Binding [Knowledge], Source={x:Static localApp:App.Localized}}"
                               Style="{StaticResource SubHeaderLabel}" />
                        </StackLayout>
                    </StackLayout>
                </Grid>


                <!-- ======= Zone de recherche ===============-->
                <HorizontalStackLayout Grid.Row="1" 
                                       Style="{StaticResource RechercherZone}"
                                       x:Name="SearchZone">
                    <Border Stroke="{StaticResource PrimaryBlue}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 24,24,24,24"
                            BackgroundColor="White"
                            Padding="0"
                            HeightRequest="50"
                            WidthRequest="600"
                            HorizontalOptions="Center"
                            x:Name="SearchBorder">

                        <Grid ColumnDefinitions="*,Auto">
                            <!-- Champ de saisie -->
                            <Entry x:Name="SearchEntry"
                                   Text="{Binding SearchQuery}"
                                   Placeholder="{Binding [SearchAuthor], Source={x:Static localApp:App.Localized}}"
                                   Style="{StaticResource TextZone}"/>

                            <!-- Bouton de recherche (icône intégrée) -->
                            <Button x:Name="SearchButton"
                                    Grid.Column="1"
                                    Style="{StaticResource SearchIconButton}">
                                <Button.ImageSource>
                                    <FontImageSource
                                        FontFamily="MaterialIcons"
                                        Glyph="&#xe8b6;"
                                        Color="White"
                                        Size="22" />
                                </Button.ImageSource>
                            </Button>
                        </Grid>
                    </Border>
                </HorizontalStackLayout>

                <!-- ========== LISTE DES AUTEURS ========== -->
                <CollectionView x:Name="AuthorsCollectionView"
                                Grid.Row="2"
                                SelectionMode="Single"
                                Margin="50,0">

                    <!-- Changer le layout pour grid avec 3 colonnes -->
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout 
                            Orientation="Vertical"
                            Span="{OnIdiom Phone=2, Tablet=4, Desktop=6, Default=3}"
                         VerticalItemSpacing="10"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <!-- Carte auteur -->
                            <Border Style="{StaticResource AuthorCard}">
                                <Grid RowDefinitions="Auto,Auto,Auto,Auto" 
                                      RowSpacing="8"
                                      Padding="15">

                                    <!-- Avatar/Initiales -->

                                    <Border Grid.Row="0"
                                        Style="{StaticResource AuthorAvatar}">
                                        <Label Text="{Binding Initiales}"
                                               Style="{StaticResource AvatarText}"/>
                                    </Border>

                                    <!-- Prénom -->
                                    <Label Grid.Row="1"
                                            Text="{Binding Prenom}"
                                            Style="{StaticResource AuthorPrenom}"/>

                                    <!-- Nom -->
                                    <Label Grid.Row="2"
                                           Text="{Binding Nom}"
                                           Style="{StaticResource AuthorNom}"/>

                                    <!-- Nombre de livres -->
                                    <Border Grid.Row="3"
                                            Style="{StaticResource BookCountBadge}">
                                        <Label Text="{Binding NombreLivres, StringFormat='{0} livre(s)'}"
                                                Style="{StaticResource BookCountText}"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- ========= GESTION DES AUTEURS ========= -->
                <Border Grid.Row="3"
                        x:Name="FormulaireLivres"
                        IsVisible="Hidden"
                        StrokeShape="RoundRectangle 20"
                        BackgroundColor="White"
                        Padding="20"
                        Margin="30,15"
                        Stroke="{StaticResource Gray300}">
                    <VerticalStackLayout Spacing="15">
                        <Label x:Name="FormTitle"
                               Text="{Binding [AuthorManagement], Source={x:Static localApp:App.Localized}}"
                               FontFamily="PlaypenSansSemiBold"
                               FontSize="16"
                               TextColor="{StaticResource PrimaryBlue}" />

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="20">
                            <VerticalStackLayout Spacing="5" Grid.Column="0">
                                <Label x:Name="PrenomFormLabel"
                                       Text="{Binding [FirstName], Source={x:Static localApp:App.Localized}}"
                                       Style="{StaticResource FormLabel}" />
                                <Entry x:Name="PrenomEntry"
                                       Text="{Binding LivreEnCours.Titre}" 
                                       Style="{StaticResource FormEntry}" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="5" Grid.Column="1">
                                <Label x:Name="NomFormLabel"
                                       Text="{Binding [LastName], Source={x:Static localApp:App.Localized}}"
                                       Style="{StaticResource FormLabel}" />
                                <Entry x:Name="NomEntry"
                                       Text="{Binding LivreEnCours.ISBN}" 
                                       Style="{StaticResource FormEntry}" />
                            </VerticalStackLayout>
                        </Grid>

                        <HorizontalStackLayout HorizontalOptions="End" Spacing="12">
                            <Button x:Name="AjouterButton"
                                    Text="{Binding [Add], Source={x:Static localApp:App.Localized}}"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding ToggleFormCommand}">
                                <Button.ImageSource>
                                    <FontImageSource FontFamily="MaterialIcons"
                                                     Glyph="&#xe145;"   
                                                     Color="White"
                                                     Size="18" />
                                </Button.ImageSource>
                            </Button>

                            <Button x:Name="ModifierButton"
                                    Text="{Binding [Edit], Source={x:Static localApp:App.Localized}}"
                                    Style="{StaticResource WarningButton}"
                                    Command="{Binding ModifierCommand}">
                                <Button.ImageSource>
                                    <FontImageSource 
                                        FontFamily="MaterialIcons"
                                        Glyph="&#xe3c9;"   
                                        Color="White"
                                        Size="18" />
                                </Button.ImageSource>
                            </Button>

                            <Button x:Name="SupprimerButton"
                                    Text="{Binding [Delete], Source={x:Static localApp:App.Localized}}"
                                    Style="{StaticResource DangerButton}"
                                    Command="{Binding SupprimerCommand}">
                                <Button.ImageSource>
                                    <FontImageSource 
                                        FontFamily="MaterialIcons"
                                        Glyph="&#xe872;"   
                                        Color="White"
                                        Size="18" />
                                </Button.ImageSource>
                            </Button>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>
            </Grid>
        </ScrollView>

        <!-- ========== BOUTON FLOTTANT “+” ========== -->
        <Button x:Name="FloatingAddButton"
                Text="+"
                Clicked="OnFloatingAddClicked"
                Command="{Binding ToggleFormCommand}"
                Style="{StaticResource FloatingAddButton}"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="0,0,20,20" />
    </Grid>
</ContentPage>
